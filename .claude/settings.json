{
  "permissions": {
    "allow": [
      "bash(mkdir *)",
      "bash(cp *)",
      "bash(cat *)",
      "bash(ls *)",
      "bash(cargo *)",
      "bash(go *)",
      "bash(docker *)",
      "write(*)",
      "read(*)"
    ]
  }
}
```
This auto-approves common file and build operations so you're not clicking "Allow" every 5 seconds. You can tune this as needed.

**3. Phase it with separate prompts**

In the VS Code chat, send one phase at a time:

> **Prompt 1:** "Read /mydata/promsketch to understand the library API and language. Then read promsketch-dropin-prompt.md. Set up the project structure at /mydata/PromSketch-Dropin with the library copied in."

> **Prompt 2:** "Implement the ingestion layer per Section 1-2 of promsketch-dropin-prompt.md — remote write receiver, scrape manager, backend forwarding, and PromSketch insertion."

> **Prompt 3:** "Implement query API and query router per Section 3-4."

> **Prompt 4:** "Build pskctl CLI per Section 7, embed vmui, and create docker-compose setup."

Each prompt within the same chat window keeps full conversation context.

**4. Add `.claudeignore`**
```
node_modules/
target/
*.log
vendor/
.git/
